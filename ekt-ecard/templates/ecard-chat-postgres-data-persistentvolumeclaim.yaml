apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "ekt-ecard.fullname" . }}-ecard-chat-postgres-pvc
  labels:
    app: {{ include "ekt-ecard.name" . }}-ecard-chat-postgres
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.ecardChatPostgres.storage }}
