apiVersion: v1
kind: Service
metadata:
  name: {{ include "ekt-ecard.fullname" . }}-ecard-chat-service
  labels:
    app: {{ include "ekt-ecard.name" . }}-ecard-chat-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.ecardChatService.port }}
      targetPort: {{ .Values.ecardChatService.port }}
  selector:
    app: {{ include "ekt-ecard.name" . }}-ecard-chat-service
