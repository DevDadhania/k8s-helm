apiVersion: v1
kind: Service
metadata:
  name: {{ include "ekt-ecard.fullname" . }}-ecard-rocketchat
  labels:
    app: {{ include "ekt-ecard.name" . }}-ecard-rocketchat
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.ecardRocketchat.port }}
      targetPort: {{ .Values.ecardRocketchat.port }}
  selector:
    app: {{ include "ekt-ecard.name" . }}-ecard-rocketchat
